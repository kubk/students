{% extends 'base.twig' %}
{% block title %}{{ isStudentRegistered ? 'Обновить профиль' : 'Зарегистрироваться' }}{% endblock %}

{% block body %}
    <div class="col-md-8 col-md-offset-2">
        <div class="panel panel-default">
        <div class="panel-heading">{{ isStudentRegistered ? 'Обновить профиль' : 'Зарегистрироваться' }}</div>
            <div class="panel-body">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}